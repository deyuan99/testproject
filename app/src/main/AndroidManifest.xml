<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ict2105.team10project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
        <activity android:name=".pushup.Pushup" />
        <activity
            android:name=".pushup.two"
            android:label="two" />
        <activity
            android:name=".mappage2"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_mappage2"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".leader" />
        <activity android:name=".challenge" />
        <activity
            android:name=".guild"
            android:label="@string/title_activity_guild" />
        <activity android:name=".play"></activity>
    </application>

</manifest>